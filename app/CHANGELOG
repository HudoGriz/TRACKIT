> v0.9.0
  + character sheet
  - lvl up
  - stats
  - char panel
  - exsustion

# v0.8.2
  + return home if token unvalid
  + dissolve combat group

# v0.8.1
  + version number added
  + code style
  + CHANGELOG tab

# v0.8.0
  + app info
  + welcome screen
  + google login moved to dashboard
  + logout
  + cookies to stay logged in (JSON scripts)
  + reload fix

# v0.7.3
  + encumberment for large pc
  + DM damage fix

# v0.7.2
  + remove input when logged

# v0.7.1
  + dynamic stats
  + exhaustion
  
# v0.7.0
  + character sheet

# v0.6.4
  + initiative fix

# v0.6.3
  + lock unlock combat group
  
# v0.6.2
  + receiving damage or healing

# v0.6.1
  + health bars to combat table
  + DM combat interface

# v0.6.0
  + combat initiative tracker

# v0.5.2
  + retrieving user input

# v0.5.1
  + saving user input

# v0.5.0
  + firebase authentication
  + character select
  - saving by exporting

# v0.4.1
  + separate interfaces

# v0.4.0
  + google login
  + character creation

# v0.3.3
  + Travel items box in inventory panel

# v0.3.2
  + item consumption/dropping
  + gold counter

# v0.3.1
  + item weight

# v0.3.0
  + inventory tab added
  + adding items
  + display items

# v0.2.1
  + calculate cost of items
  
# v0.2.0
  + saving by exporting data
  + shop tab
  + able to buy travel items

# v0.1.4
  + encumberment system

# v0.1.3
  + water refilling

# v0.1.2
  + rain caching
  + gathering

# v0.1.1
  + resource usage per size

# v0.1.0
  + Resource adding and removing
  + progress bars
  + resource usage per day
